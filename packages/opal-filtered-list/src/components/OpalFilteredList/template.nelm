@Slot (for=queryInputSlot) {
	@Slot/queryInputSlot (for=queryInput) {
		OpalTextInput/queryInput (placeholder=Поиск, clearable) {
			OpalIcon/queryInputEndIcon (class=OpalTextInput__endIcon, name=search)
		}
	}
}

@Slot/listSlot (for=list)
@section/inner {

button/control (tabindex={_tabIndex}) {
	RtSlot/contentSlot {
		'{paramText}'

		@IfThen (if=paramSubtext) {
			sub {
				'{paramSubtext}'
			}
		}
	}

	OpalIcon/iconCheck (name=checkmark)
}

}
div:bar (viewType={viewType}, hasIcon={icon |bool }, iconSize={iconSize}, buttonHide={buttonHide}) {
	OpalIcon:icon (@if=icon, name={icon}, size={iconSize})

	RnSlot:contentSlot

	button:btnHide (hide={buttonHide |not }) {
		OpalIcon:btnHideIcon (name=cross)
	}
}
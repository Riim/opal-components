@section/inner {

button/control (tabindex={_tabIndex}) {
	RtSlot/content-slot {
		'{paramText}'

		@IfThen (if=paramSubtext) {
			sub {
				'{paramSubtext}'
			}
		}
	}

	OpalIcon/icon-check (name=checkmark)
}

}